
<div class="container">
  <h1>Experiences</h1>
  <hr>
  <h2 id="excursions">Excursions</h2>
  <div class="row">
    <% @experiences.each do |experience| %>
      <div class="col-3">
        <div class="card">
          <%= experience.category %>
          <%# <% if @experience.photo.attached? %>
            <%# <%= cl_image_tag @experience.photo.key, class: "event-detail-image" %>
          <%# <% else %>
            <%= image_tag "Logo.png", alt: "Logo My-Welcome", class: "w-100 img-card" %>
          <%# <% end %>
          <p class="card-desc"><%= experience.description %></p>
          <p class="card-desc"><%= experience.local %></p>
          <p class="card-desc"><%= experience.specialty %></p>
          <p class="card-desc"><%= experience.date %></p>
          <p class="card-desc"><%= experience.price %></p>
        </div>
      <% end %>
    <% end %>
  </div>
  <hr>

  <h2 id="gastronomy">Gastronomy<a href="#experiences" class="back-to-top-link">Back to Top</a></h2>

  <hr>
  <div class="row">
    <% @experiences.each do |experience| %>
      <div class="col-3">
        <div class="card">
          <%= experience.category %>
          <%= image_tag "Logo.png", alt: "Logo My-Welcome", class: "w-100 img-card" %>
          <p><%= experience.description %></p>
          <p><%= experience.local %></p>
          <p><%= experience.specialty %></p>
          <p><%= experience.date %></p>
          <p><%= experience.price %></p>
          <!-- ... (unchanged code for Gastronomy) ... -->
        </div>
      </div>
    <% end %>
  </div>
<hr>
  <h2 id="gastronomy" >Gastronomy</h2>

  <div class="row">
    <% @experiences.where(category: "gastronomy").each do |experience| %>
      <div class="col-3">
        <div class="card">
          <%= image_tag "Logo.png", alt: "Logo My-Welcome", class: "w-100 img-card" %>
          <p><%= experience.description %></p>
          <p><%= experience.local %></p>
          <p><%= experience.specialty %></p>
          <p><%= experience.date %></p>
          <p><%= experience.price %></p>
        </div>
      </div>
    <% end %>
  </div>
<hr>
  <a href="/experiences#excursions"></a>
  <h2 id="workshops">Workshops</h2>

  <div class="row">
    <% @experiences.where(category: "workshop").each do |experience| %>
      <div class="col-3">
        <div class="card">
          <%= image_tag "Logo.png", alt: "Logo My-Welcome", class: "w-100 img-card" %>
          <p><%= experience.description %></p>
          <p><%= experience.local %></p>
          <p><%= experience.specialty %></p>
          <p><%= experience.date %></p>
          <p><%= experience.price %></p>
          <!-- ... (unchanged code for Workshops) ... -->
        </div>
      </div>
    <% end %>
