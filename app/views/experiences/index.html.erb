<div class="container">
  <h1>Experiences</h1>
  <hr>

  <h2 id="excursions">Excursions</h2>
  <div class="row">
    <% @experiences.where(category: "excursion").each do |experience| %>
      <div class="col-3">
        <div class="card">
          <!-- Display content for Excursions -->
          <%= image_tag "Logo.png", alt: "Logo My-Welcome", class: "w-100 img-card" %>
          <p><%= experience.description %></p>
          <p><%= experience.local %></p>
          <p><%= experience.specialty %></p>
          <p><%= experience.date %></p>
          <p><%= experience.price %></p>
        </div>
      </div>
    <% end %>
  </div>
  <hr>

  <h2 id="gastronomy">Gastronomy<a href="#experiences" class="back-to-top-link"></a></h2>
  <div class="row">
    <% @experiences.where(category: "gastronomy").each do |experience| %>
      <div class="col-3">
        <div class="card">
          <!-- Display content for Gastronomy -->
          <%= image_tag "Logo.png", alt: "Logo My-Welcome", class: "w-100 img-card" %>
          <p><%= experience.description %></p>
          <p><%= experience.local %></p>
          <p><%= experience.specialty %></p>
          <p><%= experience.date %></p>
          <p><%= experience.price %></p>
        </div>
      </div>
    <% end %>
  </div>
  <hr>

  <h2 id="workshops">Workshops</h2>
  <div class="row">
    <% @experiences.where(category: "workshop").each do |experience| %>
      <div class="col-3">
        <div class="card">
          <%= image_tag "Logo.png", alt: "Logo My-Welcome", class: "w-100 img-card" %>
          <p><%= experience.description %></p>
          <p><%= experience.local %></p>
          <p><%= experience.specialty %></p>
          <p><%= experience.date %></p>
          <p><%= experience.price %></p>
          <!-- ... (unchanged code for Workshops) ... -->
        </div>
      </div>
    <% end %>
  </div>
  <hr>
     <p class="float-end"><a href="#">Back to top</a></p>

  <!-- ... (other sections if needed) ... -->

</div>
